import React from "react";
import { View } from "react-native";

// Web stub — Lottie is not supported on web; renders an empty View
const LottieView = React.forwardRef(function LottieView({ style }: { style?: any }, _ref: any) {
  return <View style={style} />;
});

export default LottieView;
export { LottieView };
